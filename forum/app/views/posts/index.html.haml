#posts
  - @posts.each do |post|
    .post
      %h2.title= link_to post.title, post
      %p.date
        Published by
        = time_ago_in_words(post.created_at)
        ago by
        = post.user.email
